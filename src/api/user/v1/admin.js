/**
 * User 管理接口
 *
 * GET /user/list  — 按页加载所有用户
 * GET /user/listbycondition  — 按条件加载用户
 * POST /user/ban  — 封禁用户
 * POST /user/unban  — 解封用户
 * POST /user/setrole  — 设置用户角色
 * GET /user/getrole  — 获取用户角色
 * POST /user/setpermission  — 设置用户权限
 */
