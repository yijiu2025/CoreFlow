## 用户认证逻辑设计指南

# 核心架构概览

最新的OAuth认证架构不再仅仅是“授权”，而是一个集 身份认证（Authentication）、授权（Authorization） 和会话管理于一体的统一安全体系。其核心组件与分层如下：

1.客户端层 (Client Layer)：请求访问资源的应用程序（如Web前端、移动端App、CLI工具）。2.授权服务器层 (Authorization Server Layer)：系统的安全中枢，负责身份验证、颁发和管理令牌。它通常集成了 OpenID Provider (OP) 功能以支持OIDC。3.资源服务器层 (Resource Server Layer)：托管受保护资源的服务器（如API服务），负责验证访问令牌并执行授权决策。4.身份与权限提供者层 (Identity & Permission Provider Layer)：授权服务器的后端支撑，负责管理用户身份、角色、权限等信息（如IAM系统、LDAP、数据库）。

当前为授权服务器层负责安全中枢，负责身份验证、颁发和管理令牌

对外提供api(登录->返回)

# 安全架构核心组件

基于Spring Security 2026等现代框架
，一个健壮的OAuth架构包含以下关键安全部件：

1.授权服务器：可采用 Spring Authorization Server 等成熟开源方案，它完整支持OAuth 2.1和OIDC
。2.资源服务器：配置为OAuth2资源服务器，能够验证JWT格式的访问令牌
。3.权限管理：OAuth 2.0本身不管理用户权限的来源
。权限信息（如roles, permissions）通常由授权服务器在颁发令牌时，根据后端IAM系统的数据嵌入到JWT的Claims中
。资源服务器根据这些Claims进行细粒度的访问控制。4.客户端管理：在授权服务器中注册客户端信息，包括client_id、client_secret、支持的授权类型、回调地址等
。

# 适配不同场景的授权模式

除了核心的授权码流程，架构还需支持其他标准模式以满足特定需求：

客户端凭证模式 (Client Credentials)：适用于服务间通信（M2M）。客户端直接使用自己的凭证（client_id和client_secret）向授权服务器请求访问令牌，不涉及用户交互
。
设备码模式 (Device Code)：适用于输入受限设备。设备显示一个代码和验证URL，用户在另一设备（如手机）上访问URL并输入代码以完成授权
。
JWT持有者授权 (JWT Bearer Grant)：适用于服务账户认证。客户端使用一个预先签名的JWT作为断言来获取访问令牌，常用于CI/CD流水线或后台任务
。

# 一个最新的、生产就绪的OAuth认证结构应具备以下特征：

协议基础：基于 OAuth 2.1 和 OIDC 1.0。
安全核心：对公共客户端强制实施 PKCE，采用 JWT 作为访问令牌格式。
架构组件：清晰的授权服务器、资源服务器、客户端三层分离。
令牌策略：短期访问令牌搭配长期刷新令牌。
场景覆盖：支持授权码+PKCE、客户端凭证、设备码等主要授权模式。
权限集成：通过JWT Claims将后端IAM系统的权限信息无缝传递到资源服务器
。

┌─────────────────────────────────────────────────────────────────┐
│ OAuth 2.1 Authorization Center │
│ │
│ ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────────────┐ │
│ │ Client │ │ Token │ │ OIDC │ │ Device Code │ │
│ │ Auth │ │ Endpoint│ │ Layer │ │ Flow │ │
│ │ Endpoint │ │ │ │ │ │ │ │
│ └────┬─────┘ └────┬─────┘ └────┬─────┘ └────┬─────────────┘ │
│ │ │ │ │ │
│ ┌────┴──────────────┴──────────────┴──────────────┴─────────┐ │
│ │ Service Layer │ │
│ │ ┌─────────────┐ ┌─────────────┐ ┌──────────────────┐ │ │
│ │ │ Authorization│ │ Token │ │ Device │ │ │
│ │ │ Service │ │ Service │ │ Service │ │ │
│ │ └──────┬──────┘ └──────┬──────┘ └────────┬─────────┘ │ │
│ └─────────┼───────────────┼─────────────────┼──────────────┘ │
│ │ │ │ │
│ ┌─────────┴───────────────┴─────────────────┴──────────────┐ │
│ │ Data Store Layer │ │
│ │ ┌───────┐ ┌────────┐ ┌──────┐ ┌───────┐ ┌──────────┐ │ │
│ │ │ Users │ │Clients │ │Codes │ │Tokens │ │ Consents │ │ │
│ │ └───────┘ └────────┘ └──────┘ └───────┘ └──────────┘ │ │
│ └───────────────────────────────────────────────────────────┘ │
│ │
│ ┌───────────────────────────────────────────────────────────┐ │
│ │ Security Layer │ │
│ │ ┌──────┐ ┌──────┐ ┌────────┐ ┌──────┐ ┌──────────────┐ │ │
│ │ │ PKCE │ │ JWT │ │ RSA Key│ │OAuth │ │ Rate │ │ │
│ │ │Verify│ │Sign │ │Manager │ │Error │ │ Limiter │ │ │
│ │ └──────┘ └──────┘ └────────┘ └──────┘ └──────────────┘ │ │
│ └───────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────────┘
用户 客户端应用 授权服务器（OIDC Provider）
│ │ │
│ 1. 点击登录 │ │
│ ────────────→ │ │
│ │ │
│ │ 2. 发起授权请求 │
│ │ scope=openid profile email
│ │ ─────────────────────────→ │
│ │ │
│ 3. 用户登录并同意授权 │
│ ←───────────────────────────────────────── │
│ │ │
│ │ 4. 返回 authorization_code │
│ │ ←───────────────────────── │
│ │ │
│ │ 5. 用 code 换取 tokens │
│ │ ─────────────────────────→ │
│ │ │
│ │ 6. 返回： │
│ │ - id_token（JWT） │
│ │ - access_token │
│ │ - refresh_token（可选） │
│ │ ←───────────────────────── │
│ │ │
│ │ 7. 验证 id_token： │
│ │ - 检查签名（JWKS） │
│ │ - 检查 iss、aud、exp │
│ │ - 提取 sub、name、email │
│ │ │
│ 8. 登录成功，显示用户信息 │
│ ←──────────── │ │

┌──────────┐ ┌──────────────────┐
│ 客户端 │ │ 授权服务器 │
└────┬─────┘ └────────┬─────────┘
│ │
│ 1. 生成 code_verifier（随机字符串） │
│ code_challenge = hash(code_verifier) │
│ │
│ 2. 发起授权请求，携带： │
│ - code_challenge │
│ - code_challenge_method (S256) │
│ ─────────────────────────────────────────→ │
│ │
│ 3. 用户登录并授权 │
│ ←───────────────────────────────────────── │
│ 返回 authorization_code │
│ │
│ 4. 用 code 换取 token，携带： │
│ - authorization_code │
│ - code_verifier（原始随机字符串） │
│ ─────────────────────────────────────────→ │
│ │
│ 5. 服务器验证： │
│ hash(code_verifier) == code_challenge ? │
│ ✅ 匹配 → 返回 access_token │
│ ❌ 不匹配 → 拒绝 │
│ ←───────────────────────────────────────── │

---

## 本系统登录模式（directLogin vs 授权码 + PKCE）

本系统采用**混合模式**，按客户端类型分流：

### 一方应用（FIRST_PARTY_APP）— directLogin 直接登录

- **适用**：posecraft / firewall / admin 等本系统自有前端
- **流程**：`POST /oauth21/v1/auth/login`（或 `/mini-login`）→ 用户名密码/邮箱码 → `directLogin`
- **跳过 PKCE 和 redirect_uri 绑定**：一方应用可信，无需防授权码拦截
- **首次登录需 consent 确认**：一方应用首次登录也弹授权确认页，用户同意后写 Approval + 授予应用默认角色，后续登录静默签发（原先一方应用跳过 consent，导致用户无 Approval 记录、拿不到默认角色）
- **安全**：H5 签名验证（requireSignature）+ 异常检测 + RSA 加密传输兜底

### 三方应用 — 授权码 + PKCE + consent

- **适用**：外部第三方客户端（client_id 非 FIRST_PARTY_APP）
- **流程**：
  1. `/oauth/authorize` 授权码 + PKCE（code_challenge 校验）
  2. 用户登录后，若未授权该应用 → 返回 `consent` 确认流程（consentKey 绑定客户端指纹）
  3. `/login/consent/confirm` 确认授权 → 写 Approval
  4. `/oauth/token` 用授权码 + code_verifier 换令牌
- **强制 PKCE**：公共客户端必须，防授权码拦截
- **consent 二次确认**：consentKey 绑定发起客户端的 IP+UA 指纹，防泄露被冒用

### 边界

- `directLogin` 仅限 `FIRST_PARTY_APP`（代码 `client.client_id !== FIRST_PARTY_APP.client_id` 分支控制）
- 三方应用无法走 directLogin，必须走授权码 + PKCE
- 登录页 `/mini-login` 可被一方应用 iframe 嵌入，三方走标准 authorize 端点
