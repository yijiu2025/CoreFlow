# fullstack-rules Skill 评估报告

## 一、文件清单与规模统计

| 文件 | 行数 | 职责 |
|------|------|------|
| `SKILL.md`（主文件） | ~100 | 核心铁律 + 参考文档索引 + 修复原则 + 输出格式 |
| `references/note.md` | 318 | 注释规范（文件头/函数/行内/日志） |
| `references/naming-convention.md` | 151 | 命名规范（代码/文件/DB/权限/API/Git） |
| `references/git-patterns.md` | 431 | Git 分支模型 + 提交规范 + CR + 版本号 |
| `references/new-project.md` | 142 | 新项目初始化六步流程 |
| `references/security.md` | 165 | 安全红线汇总（XSS/SQL注入/权限/认证） |
| `references/testing.md` | 235 | 测试规范（Jest/Vitest/覆盖率/模板） |
| `references/frontend/main.md` | 194 | 前端通用规范 + 组件反例 + 数据加载反例 |
| `references/frontend/web/vue.md` | 320 | Vue 3 专用规范（权限/组件/样式/目录架构） |
| `references/backend/main.md` | 31 | 后端通用规范（极简索引） |
| `references/backend/nodejs/nodejs-fastify.md` | 276 | Fastify 代码模板（路由/DAO/模型/迁移） |
| **合计** | **~2,363** | |

---

## 二、评分（10分制）

| 维度 | 权重 | 分数 | 加权分 | 说明 |
|------|------|------|--------|------|
| **结构设计** | 15% | 9.0 | 1.35 | 主文件精简（~100行），references 按领域拆分清晰，职责边界明确 |
| **覆盖完整性** | 20% | 8.0 | 1.60 | 前后端/命名/注释/Git/日志/测试/安全/新项目模板均已覆盖。缺失：环境变量管理规范、统一错误处理模板、TypeScript 严格模式细节 |
| **可执行性** | 20% | 8.5 | 1.70 | 大量反例（note.md 3处、frontend/main.md 3处、testing.md 3处、security.md 2处）+ 模板代码（DAO/模型/路由/迁移/序列化）+ 检查清单（测试/安全/CR） |
| **反例与边界** | 15% | 8.0 | 1.20 | 6个文件含反例，覆盖注释/组件/数据加载/测试/安全。但 naming-convention.md 和 git-patterns.md 缺少反例 |
| **一致性** | 10% | 8.0 | 0.80 | vue.md 与 AGENTS.md 目录结构已统一为三层结构。但 note.md 中 `@author Claude` 应改为通用值；部分日志规范在 AGENTS.md 和 note.md 重复 |
| **可维护性** | 10% | 7.5 | 0.75 | git-patterns.md 8.3 节已明确标注卡点状态（✅/❌ 表格），但无版本号/变更记录；references 间交叉引用不够系统化 |
| **上下文效率** | 10% | 7.0 | 0.70 | 2,363 行总量中等偏高。AGENTS.md 与 skill references 存在内容重复（日志规范、目录结构、测试命令），加载时浪费 token |

**综合加权评分：8.10 / 10**

---

## 三、与前次评分对比

| 维度 | 前次(7.18) | 本次(8.10) | 变化原因 |
|------|-----------|-----------|----------|
| 结构设计 | 8.5 | 9.0 | new-project.md 从7行扩充到142行，补齐最大短板 |
| 覆盖完整性 | 7.0 | 8.0 | 新增 testing.md + security.md，补齐两大缺失领域 |
| 可执行性 | 7.5 | 8.5 | note.md/frontend/main.md 大量增加反例和检查清单 |
| 反例与边界 | 6.0 | 8.0 | 从0处反例增加到14处反例，覆盖6个文件 |
| 一致性 | 7.0 | 8.0 | vue.md 目录结构与 AGENTS.md 统一 |
| 可维护性 | 6.5 | 7.5 | git-patterns.md 8.3 节状态明确化 |
| 上下文效率 | 7.5 | 7.0 | 文件增多后总量上升，重复内容未清理 |

---

## 四、现存问题与优化建议

### P0: 消除 AGENTS.md 与 Skill References 的重复内容

**问题**：以下内容在 AGENTS.md 和 skill references 中重复出现：

| 重复内容 | AGENTS.md 位置 | Skill 位置 |
|----------|---------------|------------|
| 启动日志规范（emoji+颜色+标签） | 第379-399行 | note.md 第242-318行 |
| 前端组件三层目录结构 | 第343-360行 | vue.md 第210-270行 |
| 测试命令和覆盖率阈值 | 第321-330行 | testing.md 第228-235行 |
| 文件大小限制和拆分策略 | 第362-376行 | SKILL.md 第2行 |

**建议**：
- AGENTS.md 中保留简要说明 + 指向 skill 的引用链接
- Skill references 中保留完整规范
- 避免两处同时维护，减少不一致风险

### P1: note.md 中 `@author Claude` 应改为通用值

**问题**：note.md 第17行和第30行的示例中硬编码了 `@author Claude`，与实际使用的 MiMoCode 不符。

**建议**：改为 `@author <作者>` 或 `@author MiMo`，保持通用性。

### P2: 补充 naming-convention.md 反例

**问题**：naming-convention.md 是唯一不含反例的核心规范文件。

**建议**：在"快速对照表"前增加反例段：

```markdown
### 命名反例

// ❌ JS 变量用了 snake_case
const user_name = 'Alice'

// ❌ JS 变量用了 PascalCase
const UserProfile = {}

// ❌ 常量用了 camelCase
const maxRetryCount = 3

// ✅ 正确
const userName = 'Alice'
const userProfile = {}
const MAX_RETRY_COUNT = 3

// ❌ 模型文件用了 kebab-case
// user-session.js → 应为 UserSession.js

// ❌ 工具文件用了 PascalCase
// SessionStore.js → 应为 session-store.js
```

### P3: 补充环境变量管理规范

**问题**：环境变量在 AGENTS.md 中有完整列表，但 skill references 中无对应规范文档。AI 在新项目中不知道如何管理 `.env` 文件。

**建议**：在 `references/backend/` 下新增 `env-guide.md`，或在 `security.md` 中扩展环境变量管理部分：
- `.env` vs `.env.local` vs `.env.production` 的使用场景
- `.env.example` 的维护规则（新增变量时必须同步更新）
- 环境变量命名规范（`APP_` 前缀、`DB_` 前缀等分组约定）
- 敏感变量的分类和管理方式

### P4: 补充统一错误处理模板

**问题**：note.md 有错误处理反例，nodejs-fastify.md 有异常类列表，但缺少一个统一的错误处理最佳实践模板。

**建议**：在 `references/backend/nodejs/nodejs-fastify.md` 中增加错误处理最佳实践段：

```markdown
## 九、错误处理最佳实践

### 原则
- Controller 层：捕获所有异常，转为统一响应格式
- Service 层：抛出业务异常（BadRequest/NotFound 等）
- DAO 层：不捕获，让异常自然传播

### 模板
```js
handler: async (request, reply) => {
  try {
    const result = await xxxService.doSomething(request.body)
    return reply.result.success(result)
  } catch (e) {
    // 业务异常已携带状态码
    if (e instanceof ApiException) {
      return reply.result.fail(e.message, null, e.code)
    }
    // 未知异常记录日志
    logger.error(`[API] 未知错误: ${e.message}`, { stack: e.stack })
    return reply.result.fail('服务器内部错误', null, 500)
  }
}
```

### P5: backend/main.md 内容过于单薄

**问题**：backend/main.md 仅31行，几乎只是索引页。

**建议**：扩展为后端通用规范的完整文档，至少包含：
- 分层架构说明（Controller → Service → DAO 各层职责边界）
- 统一响应格式规范
- 异常处理规范
- 日志规范（引用 note.md）
- 数据库规范（迁移、软删除、关联）
- 安全规范（引用 security.md）

### P6: git-patterns.md 补充提交信息反例

**问题**：git-patterns.md 有"禁止"列表但缺少具体反例。

**建议**：在第156-160行的"禁止"列表后增加反例：

```markdown
### 提交信息反例

// ❌ 无意义信息
git commit -m "fix bug"
git commit -m "update"
git commit -m "wip"

// ❌ 一个提交包含多个不相关改动
git commit -m "feat(auth): 新增登录 + fix(db): 修复查询 + style: 格式化"

// ✅ 正确：一个提交只做一件事
git commit -m "feat(auth): 新增 Google OAuth2 登录"
git commit -m "fix(db): 修复用户查询时的 N+1 问题"
```

### P7: 补充 TypeScript 严格模式细节

**问题**：vue.md 技术选型中提到"TypeScript 严格模式，禁止无故使用 `any`"，但缺少具体的 strict 配置建议。

**建议**：在 vue.md 中补充 tsconfig.json 推荐配置：

```json
{
  "compilerOptions": {
    "strict": true,
    "noUncheckedIndexedAccess": true,
    "noUnusedLocals": true,
    "noUnusedParameters": true,
    "exactOptionalPropertyTypes": false
  }
}
```

---

## 五、优化优先级排序

| 优先级 | 优化项 | 工作量 | 影响 |
|--------|--------|--------|------|
| **P0** | 消除 AGENTS.md 与 Skill 的重复内容 | 中 | 减少 token 消耗，降低维护成本 |
| **P1** | note.md 中 @author 改为通用值 | 小 | 消除与实际工具名不符的硬编码 |
| **P2** | naming-convention.md 补充反例 | 小 | 提升命名规范的可执行性 |
| **P3** | 补充环境变量管理规范 | 中 | 补齐新项目初始化的关键环节 |
| **P4** | 补充统一错误处理模板 | 小 | 减少后端开发时的决策负担 |
| **P5** | backend/main.md 扩充 | 中 | 提升后端规范的完整性 |
| **P6** | git-patterns.md 补充提交反例 | 小 | 提升提交规范的可执行性 |
| **P7** | 补充 TypeScript strict 配置 | 小 | 明确 TS 严格模式的具体要求 |

---

## 六、总结

fullstack-rules skill 经过近期迭代，质量有显著提升：
- 从7行的 new-project.md 扩充到142行完整流程
- 新增 testing.md（235行）和 security.md（165行）补齐两大缺失领域
- 在6个文件中增加14处反例，覆盖注释/组件/数据加载/测试/安全
- vue.md 目录结构与 AGENTS.md 统一为三层结构
- git-patterns.md 明确标注卡点启用状态

当前评分 **8.10/10**，主要改进空间在于：消除与 AGENTS.md 的重复内容（P0）、补充环境变量管理规范（P3）、扩充 backend/main.md（P5）。

---

## 七、详细执行步骤

> 由于编辑权限限制，以下为每个优化项的具体文件修改指令。

### T1 (P0): 消除 AGENTS.md 与 Skill References 的重复内容

**文件**: `AGENTS.md`

#### 修改1: 启动日志规范（第379-399行）

**替换为**:

```markdown
## 启动日志规范

使用 emoji + 彩色文字 + 统一标签格式。完整规范见 [.claude/skills/fullstack-rules/references/note.md](.claude/skills/fullstack-rules/references/note.md) 第四节。

```js
const C = { reset: '\x1b[0m', green: '\x1b[32m', yellow: '\x1b[33m', red: '\x1b[31m', cyan: '\x1b[36m', dim: '\x1b[2m' };

console.log(`✅ [Redis] ${C.green}连接成功: ${host}:${port}${C.reset}`);
console.warn(`⚠️ [Redis] ${C.yellow}连接失败，降级到内存模式${C.reset}`);
console.error(`❌ [DB] ${C.red}缺少必要环境变量${C.reset}`);
```

标准标签：`[Redis]` `[DB]` `[Migrate]` `[Loader]` `[Guard]` `[Guard Config]` `[Firewall]` `[PBAC]` `[Seed]` `[API]` `[Auth]`
```

**改动说明**: 移除重复的级别/颜色表格（12行），保留代码示例 + 标签列表 + 添加引用链接。

#### 修改2: 前端组件目录架构（第343-360行）

**替换为**:

```markdown
- **前端组件目录架构规范**：`src/components` 下的所有组件必须按 `功能分类 -> 业务页面 -> 组件文件` 的三层结构进行放置与管理。完整规范和目录示例见 [.claude/skills/fullstack-rules/references/frontend/web/vue.md](.claude/skills/fullstack-rules/references/frontend/web/vue.md) 第七节。
  - 第一层（功能分类）：`modals/` `popovers/` `panels/` `layouts/` `cards/` `widgets/` `common/`
  - 第二层（业务页面）：`home/` `editor/` `login/` `mine/`
  - 示例：`src/components/modals/home/SettingsModal.vue`
```

**改动说明**: 移除重复的分类表格（18行），保留简要说明 + 引用链接。

#### 修改3: 测试章节（第321-330行）

**替换为**:

```markdown
## 测试

```bash
npm test                    # 运行所有测试
npm test -- --coverage      # 运行并生成覆盖率报告
```

测试文件在 `src/__tests__/` 下，使用 Fastify inject 进行集成测试。完整测试规范见 [.claude/skills/fullstack-rules/references/testing.md](.claude/skills/fullstack-rules/references/testing.md)。
```

**改动说明**: 移除重复的覆盖率阈值行（1行），添加引用链接。

---

### T2 (P1): note.md 中 @author 改为通用值

**文件**: `references/note.md`

#### 修改1: 第17行

**替换**:
```
 * @author Claude
```
**为**:
```
 * @author <作者>
```

#### 修改2: 第30行

**替换**:
```
 * @author Claude
```
**为**:
```
 * @author <作者>
```

---

### T3 (P2): naming-convention.md 补充反例

**文件**: `references/naming-convention.md`

#### 修改: 在"附：快速对照表"（第139行）之前插入

**插入内容**:

```markdown
### 命名反例

```js
// ❌ JS 变量用了 snake_case
const user_name = 'Alice'

// ❌ JS 变量用了 PascalCase
const UserProfile = {}

// ❌ 常量用了 camelCase
const maxRetryCount = 3

// ✅ 正确
const userName = 'Alice'
const userProfile = {}
const MAX_RETRY_COUNT = 3
```

```bash
# ❌ 模型文件用了 kebab-case
user-session.js    → 应为 UserSession.js

# ❌ 工具文件用了 PascalCase
SessionStore.js    → 应为 session-store.js

# ❌ Vue 组件用了 kebab-case
user-profile.vue   → 应为 UserProfile.vue
```

```

---

### T4 (P3): 补充环境变量管理规范

**文件**: `references/security.md`

#### 修改: 在"七、重型库安全"之后、"八、安全自查清单"之前插入新章节

**插入内容**:

```markdown
## 八、环境变量管理

### 文件层级

| 文件 | 是否提交到 Git | 用途 |
|------|---------------|------|
| `.env.example` | ✅ 必须提交 | 环境变量模板，列出所有变量和默认值 |
| `.env` | ❌ 禁止提交 | 本地开发环境配置 |
| `.env.local` | ❌ 禁止提交 | 本地覆盖配置（优先级高于 .env） |
| `.env.production` | ❌ 禁止提交 | 生产环境配置 |

### 命名规范

- 环境变量使用 `UPPER_SNAKE_CASE`
- 按模块分组，使用前缀：`DB_`（数据库）、`REDIS_`（缓存）、`APP_`（应用）、`OAUTH_`（认证）
- 敏感变量（密码、密钥）不设默认值，启动时校验必须存在

### 维护规则

- 新增环境变量时，必须同步更新 `.env.example`
- 敏感变量（密码、密钥、Token）不得出现在日志输出中
- 生产环境禁止使用 `DB_SYNC=true`

### .gitignore 配置

```gitignore
# 环境变量
.env
.env.*
!.env.example

# 密钥文件
*.pem
*.key
id_rsa
```

---
```

**同时** 将原来的"八、安全自查清单"改为"九、安全自查清单"，在自查清单中增加一项：
```
- [ ] 新增环境变量已同步更新 `.env.example`
```

---

### T5 (P4): 补充统一错误处理模板

**文件**: `references/backend/nodejs/nodejs-fastify.md`

#### 修改: 在"八、系统架构参考"之后追加新章节

**追加内容**:

```markdown

---

## 九、错误处理最佳实践

### 分层职责

| 层级 | 职责 | 示例 |
|------|------|------|
| Controller (路由 handler) | 捕获所有异常，转为统一响应格式 | try/catch + reply.result.fail() |
| Service | 抛出业务异常，不捕获 | throw new BadRequestException('参数错误') |
| DAO | 不捕获，让异常自然传播 | 直接 await，异常向上传递 |

### 模板

```js
handler: async (request, reply) => {
  try {
    const result = await xxxService.doSomething(request.body)
    return reply.result.success(result)
  } catch (e) {
    // 业务异常已携带状态码
    if (e instanceof ApiException) {
      return reply.result.fail(e.message, null, e.code)
    }
    // 未知异常记录日志
    console.error(`❌ [API] ${C.red}未知错误: ${e.message}${C.reset}`)
    return reply.result.fail('服务器内部错误', null, 500)
  }
}
```

### 禁止事项

- ❌ 吞掉异常：`catch (e) { }`
- ❌ 只打印不处理：`catch (e) { console.log(e) }`
- ❌ catch 后继续执行：异常后代码可能在数据损坏状态下运行
- ✅ 记录日志 + 向上抛或给用户反馈
```

---

### T6 (P5): backend/main.md 扩充

**文件**: `references/backend/main.md`

#### 修改: 在现有内容基础上扩展

**替换为**:

```markdown
# 后端规范

## 通用规范

| 领域 | 规范 |
|------|------|
| 架构 | 严格遵循分层架构（Controller / Service / DAO），各层职责单一 |
| API | 遵循 RESTful 风格，统一响应格式 |
| 数据库 | 所有表必须包含 `created_at` 和 `updated_at` 字段 |
| 安全性 | 涉及用户身份的接口必须校验权限，防范 SQL 注入和 XSS |
| 目录结构 | 按功能模块划分目录，api/models/app 内按业务子模块或应用区分文件夹 |

---

## 分层架构

### 职责边界

| 层级 | 目录 | 职责 | 禁止 |
|------|------|------|------|
| Controller | `src/api/<domain>/v1/` | 参数校验、调用 Service、返回响应 | 不直接操作数据库 |
| Service | `src/app/<name>/services/` | 业务逻辑、事务编排 | 不直接操作 HTTP 请求 |
| DAO | `src/app/<name>/dao/` | 数据访问、SQL 构建 | 不包含业务判断 |

### 调用链路

```
Request → Controller (参数校验) → Service (业务逻辑) → DAO (数据访问) → Sequelize → MySQL
                                                                    ↓
                                                              异常向上传播
```

---

## 统一响应格式

```json
{
  "code": 200,
  "message": "success",
  "data": { },
  "timestamp": 1717000000,
  "requestId": "req-xxx"
}
```

### 响应方法

```js
return reply.result.success(data)           // code: 200
return reply.result.paginated(list, total, page, pageSize)  // 分页
return reply.result.fail('错误信息', null, 400)  // 自定义错误码
return reply.result.unauth('未登录')        // code: 401
return reply.result.forbidden('权限不足')   // code: 403
```

---

## 数据库规范

### 迁移

- 所有表结构变更必须通过迁移文件管理
- 禁止在生产环境使用 `DB_SYNC=true`
- 迁移文件 `down` 必须能完全回滚

### 软删除

- 使用 `delete_version` 模式：`delete_version = id` 表示已删除
- 查询时过滤 `delete_version: 0`

### 关联

- 通过 `Model.associate = (models) => {}` 定义
- 关联查询使用 `include`，指定 `as` 和 `attributes`

---

## 技术栈目录

| 技术栈 | 参考文件 | 说明 |
|--------|----------|------|
| Node.js + Fastify + Sequelize | [nodejs-fastify.md](nodejs-fastify/nodejs-fastify.md) | API 路由、DAO、模型、迁移等代码模板 |

> 其他技术栈（如 Java / Python / Go）后续可在此目录下添加对应的子目录和规范文件。

---

## 相关规范

| 规范 | 文件 | 说明 |
|------|------|------|
| 📝 注释规范 | [note.md](../note.md) | 文件头注释、函数注释、控制台日志 |
| 🏷️ 命名规范 | [naming-convention.md](../naming-convention.md) | 代码/文件/数据库命名规则 |
| 🎯 Git 规范 | [git-patterns.md](../git-patterns.md) | 分支模型、提交信息、PR 模板 |
| 🔒 安全规范 | [security.md](../security.md) | 安全红线、XSS/SQL注入防护、环境变量管理 |
| 🧪 测试规范 | [testing.md](../testing.md) | 测试原则、覆盖率、Fastify inject 模板 |
```

---

### T7 (P6): git-patterns.md 补充提交反例

**文件**: `references/git-patterns.md`

#### 修改: 在第160行（"禁止"列表结束）之后插入

**插入内容**:

```markdown

### 提交信息反例

```bash
# ❌ 无意义信息
git commit -m "fix bug"
git commit -m "update"
git commit -m "wip"
git commit -m "调整"

# ❌ 一个提交包含多个不相关改动
git commit -m "feat(auth): 新增登录 + fix(db): 修复查询 + style: 格式化"

# ✅ 正确：一个提交只做一件事
git commit -m "feat(auth): 新增 Google OAuth2 登录"
git commit -m "fix(db): 修复用户查询时的 N+1 问题"
git commit -m "refactor(session): 抽取双令牌刷新逻辑"
```

```

---

### T8 (P7): 补充 TypeScript strict 配置

**文件**: `references/frontend/web/vue.md`

#### 修改: 在"技术选型"表格之后插入

**插入内容**:

```markdown

### TypeScript 严格模式配置

```json
{
  "compilerOptions": {
    "strict": true,
    "noUncheckedIndexedAccess": true,
    "noUnusedLocals": true,
    "noUnusedParameters": true,
    "exactOptionalPropertyTypes": false,
    "moduleResolution": "bundler",
    "target": "ESNext",
    "module": "ESNext"
  }
}
```

| 选项 | 说明 |
|------|------|
| `strict: true` | 启用所有严格类型检查 |
| `noUncheckedIndexedAccess` | 数组/对象索引返回 `T | undefined` |
| `noUnusedLocals` | 禁止未使用的局部变量 |
| `noUnusedParameters` | 禁止未使用的参数 |
| `exactOptionalPropertyTypes` | 可选属性不能赋值 `undefined`（视项目情况启用） |
```

---

## 八、执行检查清单

完成所有修改后，逐项验证：

- [ ] AGENTS.md 启动日志规范章节已精简，包含引用链接
- [ ] AGENTS.md 前端组件目录架构已精简，包含引用链接
- [ ] AGENTS.md 测试章节已精简，包含引用链接
- [ ] note.md 中 `@author` 已改为 `<作者>`
- [ ] naming-convention.md 新增反例段
- [ ] security.md 新增环境变量管理章节
- [ ] security.md 安全自查清单编号已更新
- [ ] nodejs-fastify.md 新增错误处理最佳实践章节
- [ ] backend/main.md 已扩充为完整规范
- [ ] git-patterns.md 新增提交信息反例
- [ ] vue.md 新增 TypeScript 严格模式配置
- [ ] 所有文件保存无语法错误
- [ ] 无遗漏的行号偏移导致的格式问题
