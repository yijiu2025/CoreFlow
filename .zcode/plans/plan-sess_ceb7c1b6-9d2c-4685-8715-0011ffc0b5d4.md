fullstack-rules 技能完整开发（目标：AI 读取后按需精准加载、指令零失效、与代码库零漂移）。

**一、SKILL.md 分级瘦身重构（439 行 → ≤160 行核心层）**，版本 1.9.0 → 2.0.0：
- 保留：frontmatter（whenToUse 收窄为真实触发场景，删除"股票分析/K线图"等无关触发词与未使用的 arguments 声明）、核心铁律、0-5 执行顺序、references 链接表（标注各文档加载时机）
- 下沉到新 references：`kx.md`（KX 页面描述语言全部语法/流程/规范）、`requirement-intake.md`（需求确认三轮提问 + 需求文档模板）、`anti-patterns.md`（反例速查 24 条）
- 删除"先输出 skill 加载正常"仪式句；Output Format 精简为核心模板 + 指向 code-review.md
- 收益：日常代码会话不再全量加载 439 行（省 4-6k token），KX/新项目场景按需加载对应 reference

**二、路径与事实全面对齐代码库**（严重问题）：
- 所有 `src/db|redis|auth` → `src/framework/db|redis|auth`：nodejs-fastify.md、redis.md、new-project.md、naming-convention.md、testing.md、code-review.md，以及 AGENTS.md / CLAUDE.md 的同类旧路径（双向过时一起修）
- nodejs-fastify.md：模板 `sequelize.models.Xxx` → `getModel`（消除与 AGENTS.md 铁律的矛盾）；posecraft 专属值（/stick 前缀、5177 端口、posecraft_ 表名）改为 `{{APP_PREFIX}}` 类占位符并标注示例来源
- testing.md：删除虚构的 `setupFilesAfterSetup`；testMatch 对齐仓库实际；目录示例修正；补 ESM 运行方式（--experimental-vm-modules + node bin/jest）
- new-project.md：修复两个"第二步"编号、kt/KX 术语统一、删除不存在的 product-designer 引用

**三、断链与格式修复**：vue.md 的 shared-device 链接补层级；security.md 的 git-patterns 链接改同级；naming-convention.md 第 62-75 行破损表格重写；frontend/main.md 组件目录两层 vs vue.md 三层的矛盾 → 对齐三层；文件拆分阈值统一（AGENTS.md 400 行为准，SKILL.md 700 行说法删除）。

**四、assets 清理**：删除 assets/project-template/kt/kx-lang/.git（~100 个无用文件，外部仓库 github.com/yijiu2025/kx-lang 仍保有完整内容）与互相重复的 README.md/README.en.md/README.zh.md（保留 SPEC.md + example.kx，SKILL.md/kx.md 指向 SPEC.md）。

**五、AGENTS.md 分工对齐**：保留"项目事实"与高频强制规则；注释/日志等整节内容加一行指向 note.md 的引用说明（不删——AGENTS.md 是 AI 默认加载层，保留高频规则是合理的）；stable-deviceid 清单以 AGENTS.md 版为权威，vue.md 改为引用。

**六、验证**：grep 确认无 `src/db/|src/redis/|src/auth/` 旧路径残留；逐个核对 references 相对链接可达；SKILL.md ≤160 行；git status 确认 assets 清理生效；commit。