
## 权限缓存四层防御（未提交，2026-09-16 深夜）

用户问「权限缓存键是明文 userId:appId、命名空间裸 'perm'，运维误操作或接口把用户可控数据
塞进该键空间就等价于伪造权限，有什么好办法」→ 给出四层方案 → 用户「写，写完之后 2 和 4 也实现一下」。
**四层全部实现完毕**，全量 64 套 / 910 项通过（基线 872，+38），ESLint 0 problems。

### 威胁模型重排（这一步比写代码重要）

按实际概率排序：**「权限已变更但缓存/会话未失效」才是最高频的**，且属**功能缺陷**而非攻击。
运维误操作/越权写入概率都低。所以四层重心是第 3 层（指纹），不是"防篡改"。

### 第 3 层（指纹）是核心，且**零 schema 改动**

```
指纹 = sha256( UserRole[id,updatedAt,deletedAt]
             + Role[id,updatedAt,deletedAt]      ← 角色策略变更影响该角色全体用户
             + InlinePolicy[id,updatedAt,deletedAt] )[:16]
```
行按 id 排序后拼接（SQL 不保证返回顺序，依赖顺序 → 指纹无谓抖动 → 缓存每次都判失效）。

**为什么不用 `users.perm_version` 版本列**：① `Role.policy` 改一次影响该角色下所有用户，
逐个递增必然漏（绕过 DAO 的写入不记得递增）② 多一次批量 UPDATE。指纹由数据自身推导，
不存在"忘记递增"的运维漏洞，三张表都是 `timestamps+paranoid` → 零 schema 改动。

### 关键文件

- `src/framework/auth/perm-cache.js`（新）—— `PERM_NAMESPACE='auth:perm'` / `PERM_TTL=60` /
  `buildPermKey`（sha256 截 16 位，`\u0000` 分隔防拼接歧义）/ `computePermFingerprint` /
  `getPermissions({fresh})` / `invalidatePermCache` / `warmPermissions`
- `src/framework/auth/session-perm.js`（新）—— `resolveSessionPermissions`，
  从 session.js 拆出（**权限指纹改造使 session.js 触及 1000 行硬限**）
- `session-store.js` —— 新增 `PERM_NAMESPACE` + `permStore`（第 7 个 store）

### ⚠️ 命名空间字面量不得不在两处声明

`session-store.js` 受「零兄弟模块依赖」约束（会话子模块依赖图最底层），**不能**
`import { PERM_NAMESPACE } from './perm-cache.js'`（后者依赖 permission-loader，会把底座抬高）。
故**重复声明字面量**，用契约测试钉死两者逐字一致。
→ 这是"依赖方向约束"逼出来的唯一解，不是疏忽；注释里写明了原因。

### 三处真实缺陷（不是重构，是修 bug）

1. **`token-issuer.service.js` 的预热一直是无效写**：写侧 `getStore('perm')` + 明文键
   `${user.id}:${client.client_id}`，读侧 `auth:perm` + sha256 键 —— **完全对不上**。
   预热了一直丢进黑洞。（上一轮我判断"正常路径键一致不是 bug"，那是对 JWT 读侧内部而言；
   跨 perm-cache 改造后写侧才是漏网的那个。）
2. **session.js 的 Redis 命中路径零失效机制** —— 这是"撤销权限后仍可用 30 天"的根因。
   新增指纹比对，变更了就**就地刷新会话权限**（TTL 沿用会话剩余时长）。
3. **`normalizeTs` 对 Symbol/BigInt 抛错**（探针实测抓到）：`new Date(Symbol())` 抛 TypeError。
   本仓高频缺陷类（cookie/totp/signature 各一例）的又一例。已改为类型前置判断 + try 兜底。
   → **教训：`new Date(x)` 不是安全的归一化，只有 string/number/Date 能进。**

### 第 4 层 fail-closed 的语义边界

`freshPermission: true` → 回源失败 **拒绝 403**，不是放行。与"权限读取失败降级"不同：
**敏感操作的默认答案必须是"不执行"**。
- 不入 `RUNTIME_FIELDS`（与 `requirePermission` 同为代码级声明），否则运维改 DB 就能关掉它
- 已启用：`POST /admin/iam/v1/roles/assign`、`POST /admin/iam/v1/policies`
- `applyGuardLogic` 需导出供测试驱动 → 以 `__test__applyGuardLogic` 命名导出（明示非公开 API）

### 手法：`jest.unstable_mockModule` 替换**依赖**而非复制策略

`session-perm-e2e.test.js` / `guard-fresh-permission.test.js` 都用它整体替换 perm-cache，
只注入"指纹值"和"回源结果"两个可变桩 → 零策略复制，测的是**真身**的分支逻辑。
（记忆里那条"内联副本测试会固化与真身相反的结论"的反面做法。）

### 守卫（4 个新测试文件，38 项）

- `auth/perm-fingerprint.test.js`（8）—— 含**专门断言 `underscored` 陷阱**：属性名是 `updatedAt`，
  物理列名 `updated_at` 不在 `getAttributes()` 里。方向相反的断言，防止"指纹退化成常量而缓存命中测试反而通过"
- `auth/perm-cache-contract.test.js`（17）—— 命名空间一致 / 无裸 `getStore('perm')` /
  键非明文 / session.js 不得直调 `loadUserPermissions` / `RUNTIME_FIELDS` 不含 `freshPermission`
  / iam.dao 有主动失效 + **3 项反例验证**
- `auth/session-perm-e2e.test.js`（6）—— 指纹失效端到端（含"抛错时保守回源"）
- `api/guard-fresh-permission.test.js`（7）—— 第 4 层 fail-closed 行为

**植入违规实测**：session.js 加一处 `loadUserPermissions` 直调 + `RUNTIME_FIELDS` 混入
`freshPermission` → 守卫精确报出这 2 项失败 → 撤销恢复 17/17。

### 顺手修的自伤回归

`auth-contract.test.js` 的「≤1000 行硬限」被我改红（session.js 1045 行）→
拆出 `session-perm.js`（988 行），并把新文件加入 `SESSION_FILES` 纳入同一组行数/无环约束。
**导出面仍是 24 项**（`resolveSessionPermissions` 刻意不 re-export —— 它是实现细节，
对外开放等于邀请外部绕过指纹直接改权限语义）。

### 无环验证（DFS 三色，新增两条边后必查）

```
session.js → session-perm.js → perm-cache.js → session-store.js
                             ↘ permission-loader.js
session-store.js → (空)  ← 仍是零兄弟依赖 ✅
```
20 文件 / 28 边，无环。

### 文档

`src/framework/auth/docs/PERM-CACHE-HARDENING.md` —— 四层说明 + 威胁模型表 +
"为什么不用 DB 版本列" + `underscored` 陷阱 + **强度边界**（hash 键提高的是误写/枚举成本，
不是屏障；能读 Redis 就能读指纹，指纹的作用是**检测数据变化**而非防篡改）。
��部先写文件再跑（heredoc 里 `${...}` 会被 bash 展开，之前踩过）。
`.gitignore` 只有 `.tmp-probe/`，`.tmp-*.mjs` 未被忽略 → 跑完必须 rm。

## MEMORY.md 超限清理（22:59 起）

系统提示报 MEMORY.md 超限被截断。原文件 32060 字节 / 337 行，检查发现**三处损坏**：

1. 第 43-45 行有**残句**（「录 401 / 404 → 0；已登录…」缺主语，上一行是 `.tmp-probe/` 那条，
   明显是某次追加时把一段查询次数统计的标题行吃掉了 —— 与「不要用 `cat >>`」那个坑吻合）
2. **「firewall 三维度封禁」出现两次**（一次在依赖方向节尾、一次在独立章节）
3. 章节顺序错乱（`导出位置` 排在 `依赖方向` 前面，与 `src→scripts` 那条的引用关系相反）

清理动作：按主题重排为 9 章（仓库形态 / 架构约定 / firewall 业务 / 框架陷阱 / CLI / Redis /
Fastify / Guard / 日志 / 手法），合并重复项，把「已修事故的过程叙述」降级为结论式条目，
补回被截断的「依赖方向：src 不得 import scripts」完整体。结果 **27767 字节 / 340 行**。

- 字节降幅不大是因为中文占 3 字节；实际是**结构重排 + 去重**，不是单纯删内容
- 新增第 0 章收纳「嵌套 git 仓库」与「本机 ref 失灵」两条**形态类**知识（原先散在文末）
- ⚠️ 反复确认过：**不要用 `cat >>` 追加 memory 文件**（本平台会从偏移 0 覆写）

## 终端颜色统一 + TTY 自适应（提交 `1233edb`）

用户问「根目录 index 的 `const C = {...}` 是不是不应该在这」——**对，而且比想象的严重**。

### 三层问题（逐层查出来的）

1. **重复定义**：`src/utils/colors.js` 注释写着「全系统统一使用」，已被 **14 个文件** import，
   而根目录 `index.js` 自己抄了一份一模一样的（`reset/green/yellow/red/cyan/dim` 完全同构）。
2. **降级覆盖不全（真 bug）**：`index.js` 里 `IS_TTY` 只用在 2 处（listen、catch），
   另外 4 处裸用 `C.*` —— chcp 失败、关闭超时、已安全关闭、关闭异常。
   **非 TTY 下这 4 处会泄漏 `\u001b[31m`**，而且全是"出错/关闭"这类最需要干净输出的时刻。
   探针实测：非 TTY 下 6 条消息里 4 条含 ANSI。
3. **全仓通病**：`src/` 下 16 个文件、约 86 处裸拼颜色码，**无一降级**。
   `FIX-REPORT-2026-09-15.md:166` 的 🔵-3 曾专门修过 `brute-force.js` 的 3 处，
   说明方向早被认可，只是没修彻底。

### 为什么必须在调用方降级（关键事实）

日志库**只给自己生成的"前缀"上色**（时间/级别/标签，走 `paint()` + `useColor()`），
`transports.js:85` 的 `record.msg` 是**原样拼接**的 —— 消息体里的颜色码完全由调用方负责。
落盘的 JSONL 不受影响（`file-transport.node.js:133` 的 `stripAnsiDeep` 会剥掉）。

### 修法（用户选了"改 colors.js 一处"）

改 1 个文件覆盖全部 16 个使用方 + 86 处调用点：

```js
const IS_TTY = typeof process !== 'undefined' && process.stdout?.isTTY === true;
const code = s => (IS_TTY ? s : '');
const C = { reset: code('\x1b[0m'), green: code('\x1b[32m'), ... };
export { C, IS_TTY };
```

- **改动前先验证前提**：确认没有任何代码依赖 `C` 的字符串值（无 `C[x]` 索引、
  无 `===` 比较、无 `Object.keys(C)`）→ 精搜 `src/` 唯一命中是 `table.js` 注释里的 `ESC[1m` 文字
- `index.js` 的本地 `paint` 随之删除，6 处调用点回到 `C.xxx ... C.reset`，
  与另外 14 个文件**风格完全一致**（一处一套实现比"每处各自判断"更糟）
- 行为验证用**动态 `import()` + query 绕缓存**：改 `process.stdout.isTTY` 后
  `import('./src/utils/colors.js?probe=tty')` 强制重新求值 → TTY 4/4 上色、非 TTY 0/4

### 新增守卫 `conventions/terminal-colors.test.js`（8 项）

四条规则（裸 ANSI 字面量 / 自建颜色表 / 用了 `C` 必须从 `utils/colors.js` 导入 / colors.js 必须 TTY 自适应）
+ **3 项反例验证**。例外：`src/framework/cli/table.js` 自带更强调色板（含 bold/背景色），CLI 本就跑在终端，未纳入。

**守卫当场抓出两个真实漏网**（我自己先前的排查没发现，因为只搜了 `\x1b[` 字面量）：

- `src/framework/db/index.js:14` —— 本地 `const C = { reset, red }`，用于**模块级 fail-fast**
  （缺 DB 环境变量时退出），非 TTY 下泄进容器日志
- `src/framework/loader/registry/09-notice.js:12` —— 本地 `const C = { reset, cyan }`

→ **教训：搜"字面量"只能抓一种写法，抓"自建颜色表"要按结构特征搜**
（同时含 reset + ≥2 个 3x 前景色码）。

### 手法：守卫必须植入违规实测

新守卫写完先跑基线，再 `printf >>` 植入违规确认**真会红**（这次精确报出 2 项失败并指名文件），
然后撤销。撤销时备份放**当前目录**（`.tmp-xx.bak`）而非 `/tmp` —— 本平台 `/tmp` 路径转换会出问题，
且 `cp ... || cp ...` 的 fallback 链容易拿到空备份。

验证结果：全量 **60 套 / 870 项通过**（比改动前 +1 套 8 项），ESLint 0 problems。

## IS_TTY 收敛：index.js 改为复用 colors.js（提交 `ba356b2`）

用户追问「index 里面的是不是可以移除掉了」——**不能删，但应该改成复用**。

### 判断依据

`IS_TTY` 在 `index.js` 里只剩**一个**真实用途：决定要不要执行 `chcp 65001`。
这与颜色**是两件事**：

- 颜色问题 → `chcp` 无关，已由 `colors.js` 按 TTY 降级，直接插值 `C.xxx` 即可
- `chcp` 改的是**终端代码页**，属"非颜色类的终端能力判断" —— 无终端的进程执行它是无意义的

所以正确做法是保留判断、但**复用** `colors.js` 已导出的 `IS_TTY`，
而不是删掉整段（删掉会导致 docker/pm2 里也去跑 chcp）。

### 改动前的验证（必须先做）

两处判断写法其实**不同**，需确认等价才敢替换：

```js
index.js  : process.stdout.isTTY === true
colors.js : typeof process !== 'undefined' && process.stdout?.isTTY === true
```

探针在 `true` / `undefined`（管道）/ `false` 三种取值下对比 → **三者取值完全一致**，可安全复用。
（差异只在浏览器/无 process 的边缘环境，Node 服务端入口恒等价。）

### 结果

- `index.js`：删本地读取，改 `import { C, IS_TTY } from './src/utils/colors.js'`，
  悬空的 JSDoc 改写成挂在 `chcp` 段上的行注释（解释"为什么这里用 IS_TTY 而非颜色开关"）
- `colors.js`：文档补充 `IS_TTY` 的导出用途
- **全仓 `process.stdout.isTTY` 读取点：2 处 → 1 处**（仅 `colors.js`）

### 守卫加第 5 条规则

「TTY 判断只允许出现在 `utils/colors.js`，其它处须 `import { IS_TTY }`」
+ 第 4 项反例（**含"注释里提到 isTTY 不算违规"** —— 先剥块注释/行注释再匹配，
否则本测试文件的说明文字会自己把自己判违规）。

- 覆盖范围扩到**项目根**（`collectRootScope()`），因为 `index.js` 在 `src/` 之外
- 植入 `const _PLANTED_TTY = process.stdout.isTTY` 实测 → 精确报出该规则失败 → 撤销恢复

验证结果：全量 **60 套 / 872 项通过**（+2 项即新增的两条规则），ESLint 0 problems。
