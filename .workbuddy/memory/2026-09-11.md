# 2026-09-11

## 日志框架抽包 wb-log + 实例级配置

- 新建 packages/log（包名 wb-log，workspaces 已含 packages/*）：核心全部迁入 src/{config,context,logger,transports,sanitize,index}.js + index.d.ts；浏览器安全（config 用 globalThis.process 检测、node:fs 只在 file-transport.node.js 经动态变量说明符加载，浏览器自动降级仅控制台，log.file.* 安全 no-op）
- 新能力：createLogger(tag, options) 实例配置 { level, console, file: true|false|{name,dir}, debug: true|false }（优先级 实例>模块规则>全局）；log.config(patch) 运行时热更新返回 this；configureLog(patch) 全局编程覆盖（level/dir/fileName/console/file/pretty/showDev/debugKeywords）
- 文件命名：LOG_FILE_NAME / configureLog({fileName}) 全局前缀（默认 app → app-日期.log）；实例 file:{name:'pay'} → pay-日期.log + pay-error-日期.log，可指定独立 dir
- src/framework/log 变薄适配层：index.js 转发包导出 + initLogErrorTraps（traps.js 改从包导入）；旧 config/context/logger/transports/sanitize.js 已删除；全部存量 import 路径不变
- 注意：transports.js 用了 top-level await 动态加载 node 文件通道；README 双份（框架文档 src/framework/log/README.md 重写三节配置法/文件命名/复用说明，包文档 packages/log/README.md）
- 验证：冒烟 6 场景（实例配置/独立文件/运行时切换/configureLog/依赖隔离/包直连）+ jest 97 tests + eslint（新包干净；3 error 为存量）

## 零配置打印 + 文件名全量配置

- 包新增零配置入口：export { logger as log }——import { log } from 'wb-log' 直接打印（tag 'app'，跟随全局配置，log.config 可对默认 logger 热更新）；logger/Logger 静态调用保留
- 文件配置全量化（实例 file 对象 / configureLog file 对象 / 环境变量三层一致）：name 前缀、dir 目录、ext 扩展名（默认 .log）、date 日期后缀（默认 true，false=单文件不滚动）、error 错误文件（默认 true，false 关闭，字符串=自定义前缀）；env 对应 LOG_FILE_EXT / LOG_FILE_DATE=off / LOG_ERROR_FILE=off
- 错误文件命名规则：全局默认前缀时沿用旧约定 error-日期.log，自定义 name 时为 <name>-error；file-transport._open 改为接收完整文件名
- 验证：4 场景冒烟（零配置/实例全量/全局对象/旧命名回归）+ jest 97 + eslint 干净
