# 2026-09-12

## wb-log 日志框架收尾（设计评分 + 文档重写）

- 重写 `src/framework/log/README.md`：改为场景化渐进结构（10秒上手 → 按场景查用①-⑥ → 环境变量总表 → 内置能力 → 前端复用 → 兼容旧API → 源码），补全 `keepDays`/`LOG_KEEP_DAYS` 文档。
- 同步更新 `packages/log/README.md`：补 keepDays、零配置 `log` 出口、双出口 exports 条件路由说明、同步落盘说明。
- 最终验证：8 套件 109 测试全部通过（含 wb-log 12 项新单测）；ESLint 对 packages/log + framework/log 零告警。
- 设计自评（用户要求的打分）：功能完整 9/10、API 设计 8.5/10、文档 8.5/10（本轮提升）、可移植性 9/10、测试 9/10、性能 8/10，综合 8.8/10。
- 命令备忘：Windows Git Bash 下跑本仓库测试用 `node --experimental-vm-modules ./node_modules/jest/bin/jest.js --testPathPatterns "..."`（直接 `npx jest` 会丢 ESM 标志）。

## 项目约定：日志一律用包名 `wb-log` 导入（重要）

- 禁止跨目录深层路径导入（如 `../../packages/log/src/index.node.js`）。统一 `import { createLogger } from 'wb-log'`。
- `wb-log` 由 npm workspaces 软链（`node_modules/wb-log -> packages/log`），**不要写进 dependencies**，否则会被线上同名包覆盖。
- 已改：`src/framework/log/index.js`（`export * from 'wb-log'`）、`src/framework/log/traps.js`、`src/__tests__/framework/log/wb-log.test.js`。
- `src/framework/log/index.js` 仍是全项目唯一日志入口（业务代码 90 个文件的既有导入路径不变）。
- 验证：109 测试全绿 + ESLint 零告警 + `node index.js` 启动正常。

## wb-log 开源发布（GitHub + npm）

- 上传 GitHub：`packages/log` 初始化为独立 git 仓库（main 分支），推送到 https://github.com/yijiu2025/log
  - 首次提交 `6c8aeb1`（12 文件 / 19.1 kB）；重命名后提交 `11e4d39`
  - 主仓库 `.gitignore` 已排除 `packages/log/`（它是嵌套独立仓库，避免误提交）
- 发布 npm：**包名 `wb-log` 被 npm 拒绝**（403：与已有包 `wblog` 名称过于相似），改用 scope 名 **`@qirly/wb-log`** 发布成功
  - `@qirly/wb-log@0.1.0`，MIT，19.1 kB / 12 文件
- 配套文件新增：`LICENSE`(MIT)、`.gitignore`、`.npmignore`；`package.json` 补 `repository`/`homepage`/`bugs`/`files`
- 同步改名：`src/framework/log/index.js`、`traps.js`、`wb-log.test.js` 及 README 全部改为 `@qirly/wb-log`；workspace 软链重建为 `node_modules/@qirly/wb-log`
- 验证：109 测试全绿 + 包名解析正常（18 个导出）
- 踩坑备忘：
  - 备份目录不能留在 `packages/` 下，否则 npm workspaces 报 `EDUPLICATEWORKSPACE`（同名多路径）
  - 环境变量无法设置含 `/` 的 npm 配置项，用 `npm publish --userconfig ./.npmrc.tmp` 临时配置文件（用完即删）
  - npm token 失效表现为 `PUT` 返回 404/403，用 `curl -H "Authorization: Bearer <token>" https://registry.npmjs.org/-/whoami` 判别
