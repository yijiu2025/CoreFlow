# 2026-07-01

## posecraft / HomeView.vue — 搜索框悬浮重构（第一轮）

- 将搜索框从文档流中移出：改为 `position: fixed` 的 `.search-float-group`，不挤占内容
- 在 main-content-area 顶部加 `.search-hero-placeholder`（88px）撑开占位空间
- 滚动吸附动画：width 640→220px + opacity fade，用 cubic-bezier 过渡

## posecraft / HomeView.vue — 小红书布局对标重构（第二轮）

- 彻底废弃 fixed + placeholder + searchProgress + scroll-listener 方案
- 改为 `.search-sticky-header`（`position: sticky; top: 72px`），搜索框铺满内容区
- 搜索框左侧改为 `+` 按钮（SVG），有竖线分隔，宽度 100%，圆角矩形（12px）
- channel-tab 紧贴搜索框下方，与搜索框共享 sticky 头部（无 gap）
- suggestion 面板改为 `position: absolute`，不挤占布局
- 移除 topnav 内嵌搜索框（nav-search-inline 改为 display: none）
- 瀑布流内容区 padding 移到 `.content-container` 自身（padding: 20px 32px 0）

## posecraft / HomeView.vue — 响应式布局重构（第三轮）

- 小屏幕(≤1024px)：侧边栏隐藏（`transform: translateX(-100%)`），点击汉堡按钮展开 + overlay 遮罩
- 搜索框初始位置：`main-content-area { padding-top: max(0, calc(40vh - 72px - 64px)) }`，使搜索框位于视口~40%处
- 滚动行为：IntersectionObserver 监测 `.search-sentinel`（rootMargin: -72px），一旦搜索框进入 sticky 位置则 topnav 搜索框渐显
- topnav 恢复 `.nav-search-inline`（opacity + translateY 过渡），滚动后 `.visible` 类控制显示
- 小屏 topnav：`🔍` 图标按钮（`.nav-search-mobile`）位于通知按钮左侧，桌面端隐藏，点击跳转 `/search`
- 小屏投稿按钮隐藏文字只留图标，VIP 标识隐藏，头像缩小
- 平板端(769-1024px)：独立微调内边距
- 小手机端(≤480px)：进一步缩小间距和搜索框高度

## posecraft / HomeView.vue — 搜索框随内容上滚 + 回到顶部（第四轮）

- 去除 `.search-sticky-header` 的 `position: sticky; top: 72px` → 改为 `position: relative`，搜索框+频道tab正常文档流，随内容自然上滚
- 保留 IntersectionObserver 监测 sentinel（rootMargin: -72px），搜索框完全滚出视野后 topnav 显示紧凑搜索
- 右下角新增 `.back-to-top-btn`（position: fixed, bottom: 32px, right: 32px），scrollY > 500 时出现，悬停变红色品牌色
- 小屏放大镜按钮改为 `v-show="isMobile && showNavSearch"`，仅在搜索框滚出后出现（而非始终可见）
- 暗色模式下回到顶部按钮样式适配
- 移动端媒体查询中清理了残留的 `top: 56px/52px` sticky 定位声明

## posecraft / HomeView.vue — 三搜索元素严格互斥（第五轮）

- 将 sentinel 从 search-sticky-header 顶部移到底部（channel-container 之后），确保整个搜索头完全滚出后才触发 topnav 搜索切换
- `nav-search-inline` 显式绑定 `:class="{ visible: showNavSearch && !isMobile }"`，桌面端专属
- `nav-search-mobile` 保持 `v-show="isMobile && showNavSearch"`，移动端专属
- 三个搜索元素（主搜索框 / topnav紧凑搜索 / topnav放大镜图标）任意时刻只有一个可见

## posecraft / HomeView.vue — 搜索框对标小红书细节（第六轮）

- 搜索框 HTML 结构重构：改为 flex column，两行布局
  - 第一行 `.search-row-input`：输入框（始终显示）
  - 第二行 `.search-row-actions`：+ 按钮 + 分隔线 + "问点点 AI"（聚焦时展开）
  - 浮动搜索按钮 `.search-btn-float`：position: absolute 定位在右下角，深色圆形，hover 变品牌红
- 搜索框默认 46px 高，聚焦时展开到 ~82px，带 `transition: all 0.3s ease` 平滑过渡
- 圆角从 12px 增大到 16px，更卡片化
- 推荐面板 `.search-suggestions` 移入 `.search-hero-bar` 内部，使用 `position: absolute` + `top: calc(100% + 6px)` 紧贴搜索框下方
- 推荐面板圆角从 16px 增大到 20px，阴影更柔和 `0 16px 40px rgba(0,0,0,0.12)`
- 推荐面板顶部加 `border-bottom` 分隔线，`suggest-header` 用 padding 而非 margin 控制间距
- 推荐项 `suggest-item` padding 从 8px 4px 增大到 10px 8px，更像列表项
- `suggest-fade` 动画增加 `translateY(-6px)` 入场效果

## posecraft / HomeView.vue — 搜索框推荐一体化 + 顶部操作栏 + 分类居中（第七轮）

- "猜你想搜"推荐面板移入搜索框内部：原 `.search-suggestions`（absolute 外部定位）→ `.search-suggestions-inner`（搜索框内部，`overflow: hidden` + `max-height` 过渡展开）
- 推荐面板用水平线 `.suggest-divider` 与搜索输入区隔开，而非独立边框
- 操作栏（+ 和 问点点 AI）在页面顶部时始终显示：`v-show="searchFocused || !showNavSearch"`，CSS 加 `.at-top` class 支持
- 搜索按钮 `.search-btn-float` 移入 `.search-row-input` 内部，用 `top: 50%; transform: translateY(-50%)` 垂直居中
- `.channel-container` 加 `justify-content: center`，分类 tab 居中显示
- `.search-hero-bar` 设 `overflow: hidden`，推荐面板通过 `max-height: 0 → 300px` 展开/收起
- 清理所有旧 `.search-suggestions`（absolute 定位）CSS 及媒体查询中的 left/right 覆盖

## posecraft / HomeView.vue — topnav 搜索框居中（第八轮）

- 将 `.nav-search-inline` 从 `.nav-left` 内移出，作为 `.top-nav` 的直接子级
- CSS 改为 `position: absolute; left: 50%; top: 50%; transform: translate(-50%, -50%)` 水平垂直居中
- 宽度 360px，最大 40vw，通过 opacity + translateY 动画显隐
- `.nav-left` 不再包含搜索元素，保持左侧按钮和标题的自然布局

## posecraft / HomeView.vue — 推荐面板浮层化，不挤占下方内容（第九轮）

- `.search-hero-bar`：`overflow: hidden` → `overflow: visible`，让绝对定位的浮层不被裁剪
- `.search-suggestions-inner`：从文档流 `max-height` 展开 → `position: absolute; top: 100%; z-index: 20;`，浮在搜索框下方
- 浮层自带 `background + border + border-radius(0 0 16px 16px) + box-shadow`，与搜索框视觉衔接
- 动画从 `max-height` 切换改为 `opacity + translateY(-8px → 0)`，不参与布局计算
- `suggest-expand` transition 类同步更新

## posecraft / HomeView.vue — 推荐面板回到搜索框内部 (第十轮)

- 输入行和操作栏包入 `.search-content` 包装 div（`position: relative; z-index: 2`），决定搜索框文档流高度
- `.search-suggestions-inner` 移入 `.search-content` 内部，`position: absolute; top: 100%`，定位在内容区下方
- 紧贴搜索框内容底部展开，不挤占下方布局，视觉上属于搜索框内部
- 背景色显式设为 `#ffffff`（暗色 `#18181b`），与 hero-bar 保持一致

## posecraft / HomeView.vue — 推荐面板文档流 + 同一框 + 负 margin 抵消（第十一轮）

- 去除 `.search-content` 包装层，推荐面板直接作为 `.search-hero-bar` 的子元素，走正常文档流
- `.search-hero-bar`：`overflow: hidden` + `border-radius: 16px`，输入区、操作栏、推荐面板全在同一个圆角框内
- `.suggest-divider` 水平线分隔输入区和推荐面板
- `.search-hero-bar.focused`：`margin-bottom: -130px; z-index: 20;`，负 margin 精确抵消推荐面板高度，下方分类 tab 不位移
- `.search-suggestions-inner`：回到 `max-height: 0 → 200px` + `opacity` 过渡动画

## posecraft / HomeView.vue — 搜索框宽度限制（第十三轮）

- `.search-hero-bar` 加 `max-width: 640px; margin: 0 auto;`，桌面端搜索框收窄居中

## posecraft / HomeView.vue — topnav 元素协调 + 760px 放大镜断点（第十四轮）

- 放大镜图标断点改为 `windowWidth < 760`（JS v-show），不再依赖 `isMobile`（≤1024）
- `nav-search-inline` 显隐条件改为 `showNavSearch && windowWidth >= 760`，填补 760-1024px 区间搜索缺失
- 新增 `@media (max-width: 759px)`，`nav-search-mobile { display: flex }` + `nav-search-inline { display: none !important }`
- 从 `≤1024px` 媒体查询中移除 `nav-search-mobile`/`nav-search-inline` 的样式，避免 CSS + JS 双重控制冲突
- 760-1024px：topnav 居中搜索框（max-width: 40vw 自适应）、无放大镜、侧边栏隐藏、title 隐藏
- < 760px：放大镜图标、无 inline 搜索、侧边栏隐藏、全紧凑布局
- ≥1024px：居中搜索框、侧边栏显示、title 显示、完整桌面布局

## posecraft / HomeView.vue — 推荐面板绝对定位 + 无缝拼接（第十二轮）

- 去除 `margin-bottom: -130px` 硬切换，彻底消除下方内容闪烁跳动
- `.search-suggestions-inner`：`position: absolute; top: calc(100% - 1px); left: -1.5px; right: -1.5px;`，完全脱离文档流
- 通过 `opacity: 0 → 1` + `transform: translateY(-12px → 0)` 实现 0.22s 平滑动画
- `.search-hero-bar.focused`：`border-radius: 16px 16px 0 0`（底部圆角打开）
- 推荐面板：`border-radius: 0 0 16px 16px`（底部圆角闭合），与搜索框共享相同的 border 和 box-shadow
- 搜索框溢出改为 `overflow: visible`，不再裁剪浮层
- 移除 `:focus-within` 样式（与 `.focused` 类功能重复）
- 删除 `suggest-expand` transition 类，改用 `.search-suggestions-inner` 自身的 CSS transition
- 模板中 `<transition>` 换成 `v-show` + CSS 动画控制

## posecraft / HomeView.vue — topnav 搜索框 flex 自适应，彻底消除重叠（第十五轮）

- `.nav-search-inline`：从 `width: 0 → 360px` 硬宽度过渡 → `flex: 1; min-width: 0; max-width: 360px` 自适应剩余空间
- 去掉 `overflow: hidden`、`transition: width`，仅保留 `opacity` 过渡
- 1025px 时搜索框自动适应 nav-left(200px) 和 nav-right(300px) 之间的剩余空间，不越界不重叠
- `.inline-search-bar` 加 `max-width: 360px`，与父容器约束形成双层保护

## posecraft / HomeView.vue — 聚焦推荐面板覆盖分类 tab 修复（第十六轮）

- 用户将 `.channel-container` 的 `margin-top` 从 `4px` 改为 `40px`，增大搜索框与分类 tab 间距
- 第一次尝试用 `margin-top: calc(40px + 300px)` 让位 → 反而把内容挤下去了，回退
- 最终方案：推荐面板是 `position: absolute` 浮层，`.search-hero-bar.focused { z-index: 20 }` 确保整个搜索框（含推荐）叠在分类 tab 之上
- 推荐面板覆盖分类 tab 但不挤占布局，这是浮层应有的行为
- 清理 `hero-focused` class 和 `transition: margin-top` 残留
